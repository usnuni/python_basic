
#변수 : 데이터에 이름을 붙여준다.
# a = [1,2,3,4,5], "=(assignment)"기호를 사용하여 생성

#변수가 가리키는 메모리의 주소를 확인 : id(a) - 별로 알 필요 없음
#(컴퓨터가 메모리를 저장해 둔 장소의 주소)

#각 메모리에는 '주소'라는 고유한 식별값이 있다.
#변수는 그 식별값에 붙이는 이름이다.

a = 10
print(id(a)) #메모리의 식별값 확인하기

# 변수의 이름짓기 규칙
# 1. 문자, 숫자, 그리고 단 하나의 특수기호(_)로 이루어진다.
# 2. 숫자가 맨 앞 글자여서는 안 된다.
# 3. 키워드와 겹치는 이름을 써서는 안 된다.
# 4. (권장) 가능하면 영어를 사용하고, 의미가 있는 단어를 쓰자!


# 변수는 이름표이므로, 데이터를 변경해가며 쓸 수 있다.
fruit = "자몽"
fruit = "메론" #메론으로 바뀜


#리스트 복사_1
a = [1, 2, 3]
b = a[:] #슬라이싱 사용
a[1] = 4
# a리스트의 값을 바꾸어도 b리스트에 영향을 미치지 않는다.
print(a)
print(b)

#리스트 복사_2
from copy import copy
c = [1, 2, 3]
d = copy(c)
#변수가 같은 값을 가지면서 다른 객체를 생성
print(c)
print(d)

print(c is d) # 서로 다른 메모리이므로 false 출력



#변수를 만드는 여러가지 방법
x, y = ('my', 'love') # 튜플을 사용, (x, y)의 괄호 생략 가능
print(x)
print(y)

j = k = 'baby' #여러개의 변수에 같은 값을 대입
print(j)
print(k)

